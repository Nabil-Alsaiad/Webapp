import React from "react";
import Logo from "./core/Logo";

/**
 * @returns {React.JSX.Element}
 */
function Header() {
  return (
    <header>
      <Logo />
    </header>
  );
}

export default Header;
